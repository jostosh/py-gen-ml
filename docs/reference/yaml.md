# `YamlBaseModel`

Base class for generated config models. Use it (via the generated subclasses) to load YAML, merge patches, resolve references, and apply CLI overrides in your training code.

::: py_gen_ml.YamlBaseModel
